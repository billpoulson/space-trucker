{
  "name": "@space-truckers/client",
  "version": "1.0.0",
  "scripts": {
    "dotenv-pull": "npx dotenv-vault@latest pull",
    "dotenv-push": "npx dotenv-vault@latest push",
    "generate-env": "nodemon --watch .env --exec tsx generate-env.ts"
  },
  "dependencies": {
    "@types/three": "^0.172.0",
    "three": "^0.172.0"
  }
}
